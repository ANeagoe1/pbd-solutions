import java.util.Scanner;

public class elseandif {
    public static void main(String[] args) {
    
    Scanner sc = new Scanner(System.in);
    
    System.out.println("Enter the number of people.");
    int people = sc.nextInt();
    
    System.out.println("Enter the number of cars.");
    int cars = sc.nextInt();
    
    System.out.println("Enther the number of buses");
    int buses = sc.nextInt();
    
    if (cars > people) {
        System.out.println("We should take the cars.");
    } else if (cars < people) {
        System.out.println("We shouldnot take the cars.");
    } else {
        System.out.println("We can't decide.");
    }
    
    
    if (buses < cars) {
        System.out.println("That's too many buses.");
    } else if (buses < cars) {
        System.out.println("Maybe we could take the buses.");
    } else {
        System.out.println("We still can't decide ");
    }
    
    
    if (people < buses) {
        System.out.println("All right, let'sjust take the buses.");
     } else {
        System.out.println("Fine lets stay home and eat ice cream then.");
     }
  }
}   
